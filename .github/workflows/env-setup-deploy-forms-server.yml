name: Env Setup - Deploy Forms Server
run-name: Env Setup - Deploy forms server from ${{ github.ref_name }}(form.io Git reference ${{ inputs.formioRepoRef }}) to ${{ inputs.environment }}
permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment the formio is deployed."
        required: true
        type: environment
      formioRepoRef:
        description: "Form.io Git reference, please refer https://github.com/formio/formio for commits or tags."
        type: string
        required: true
        default: "v4.3.2"

jobs:
  deploy:
    name: Deploy Forms Server
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    env:
      FORMS_NAME: forms
      NAMESPACE: ${{ secrets.OPENSHIFT_ENV_NAMESPACE }}
      BUILD_NAMESPACE: ${{ vars.BUILD_NAMESPACE }}
      FORMIO_SOURCE_REPO_REF: ${{ inputs.formioRepoRef }}
      HOST_PREFIX: ${{ secrets.HOST_PREFIX }}
      MONGODB_URI: ${{ secrets.MONGODB_URI }}
      FORMIO_ROOT_EMAIL: ${{ secrets.FORMS_SA_USER_NAME }}
      FORMIO_ROOT_PASSWORD: ${{ secrets.FORMS_SA_PASSWORD }}
      FORMIO_CPU_REQUEST: ${{ vars.FORMIO_CPU_REQUEST }}
      FORMIO_MEMORY_REQUEST: ${{ vars.FORMIO_MEMORY_REQUEST }}
      FORMIO_MEMORY_LIMIT: ${{ vars.FORMIO_MEMORY_LIMIT }}
      FORMIO_REPLICAS: ${{ vars.FORMIO_REPLICAS }}
    steps:
      - name: Install CLI tools from OpenShift Mirror
        uses: redhat-actions/openshift-tools-installer@v1
        with:
          oc: "4"
      - name: Print env
        run: |
          echo NAMESPACE: $NAMESPACE
          echo HOST_PREFIX: $HOST_PREFIX
          echo OC CLI Version: $(oc version)
      # Checkout the PR branch
      - name: Checkout Target Branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref_name }}
      # Log in to OpenShift.
      - name: Log in to OpenShift
        run: |
          oc login --token=${{ secrets.SA_TOKEN }} --server=${{ vars.OPENSHIFT_CLUSTER_URL }}
      # Deploy Forms
      - name: Deploy Forms server
        working-directory: "./devops/"
        run: |
          make oc-deploy-forms
