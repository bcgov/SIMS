export enum InstitutionUserType {
  admin = "admin",
  locationManager = "location-manager",
  user = "user",
}

export enum InstitutionUserRole {
  signingAuthority = "legal-signing-authority",
  primaryContact = "primary-contact",
}
