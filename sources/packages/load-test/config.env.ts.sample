/**
 * This file should be renamed to config.env.ts and have the missing
 * values provided. They are intensionally hidden because they are
 * considered sensitive information.
 */

/**
 * SIMS API base url.
 */
export const BASE_API_URL = "TO BE CHANGED";

/**
 * Keycloak URL to retrieve the token.
 */
export const KEY_CLOAK_TOKEN_URL = "TO BE CHANGED";

/**
 * Institution user admin (e.g. SIMS_COLLF on DEV).
 */
export const INSTITUTION_ADMIN_USER = "TO BE CHANGED";

/**
 * Institution user admin password (e.g. SIMS_COLLF on DEV).
 */
export const INSTITUTION_ADMIN_PASSWORD = "TO BE CHANGED";

/**
 * Forms server URL.
 */
export const FORMS_URL = "TO BE CHANGED";
/**
 * Forms server token system user.
 */
export const FORMIO_ROOT_EMAIL = "TO BE CHANGED";
/**
 * Forms server token system password.
 */
export const FORMIO_ROOT_PASSWORD = "TO BE CHANGED";